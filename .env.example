# Copy to .env and fill in. Vite only exposes VITE_-prefixed vars to the client.
# If these are absent, the app runs fully local (localStorage only).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
